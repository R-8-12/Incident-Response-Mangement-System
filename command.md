# pip freeze > requirements.txt --> TO ADD

# pip install -r requirements.txt --> TO INSTALL

# mysql -u root -p dbmsproject -----> connect mysql


# pip freeze | ForEach-Object {pip uninstall -y $_}
